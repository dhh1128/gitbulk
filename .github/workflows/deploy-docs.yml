# Build the Zensical docs site and deploy it to GitHub Pages
# (https://dhh1128.github.io/gitbulk/).
#
# One-time setup (cannot be done by the workflow): in the repo,
# Settings -> Pages -> Source = "GitHub Actions". The first deploy fails
# without it. (GitHub Pages also requires the repo to be public, or a plan
# that allows Pages on private repos.)
#
# Actions are pinned by full commit SHA (node24 / composite runtimes,
# verified 2026-06-03; SHAs resolved 2026-06-03) with a `# vN` comment so
# the version stays legible and Dependabot can bump them. A mutable @vN tag
# can be silently retargeted to malicious code (supply-chain hardening),
# matching ci.yml / release.yml.
name: Deploy Docs

on:
  push:
    branches: [main]
    paths:
      - 'docs/**'
      - 'zensical.toml'
      - '.github/workflows/deploy-docs.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# One deploy at a time; let an in-progress deploy finish rather than cancel it.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10  # v6

      - name: Install uv
        uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39  # v8.2.0
        with:
          enable-cache: true

      - name: Configure Pages
        uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d  # v6

      - name: Build site with Zensical
        run: uv run --group docs zensical build --clean

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9  # v5
        with:
          path: site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128  # v5
