# prune-worktrees: silent 30-45s open-PR fetch + opaque HTTP 502 abort
kind: todo
tags: bug
created: 2026-06-08T22:10Z

- 2026-06-08T22:10Z Reported 2026-06-08 by user. 'gitbulk prune-worktrees' on a 190-repo fleet: after the per-repo-checks progress finishes, the screen is BLANK for 30-45s, then aborts with: 'open-PR fetch failed: gh exhausted 3 attempts: gh: HTTP 502'. Root: prune_worktrees.py:714 calls gh.my_open_prs([190 slugs], author=None) with NO on_progress callback. That call runs ~4 sequential paginated GraphQL searches (chunk=50 repos, each PR pulling reviewThreads(100)+labels+commits) = the silent window. A 502 (GitHub bad-gateway on an expensive search) is retried 3x by _run then raised; the whole-scope fetch failure is structural-by-design (can't reason about open-PR heads -> abort rather than mis-classify+prune). Two asks: (1) progress during the fetch; (2) echo logs/where-the-logs-are with the error for diagnosis. NOT changing the abort-is-structural safety (skipping a failed chunk could prune a worktree whose branch has an open PR). Possible follow-up: chunk=50 may itself be why GitHub 502s on this fleet (query too expensive) -> consider smaller chunk / trimmed selection.
- 2026-06-08T22:21Z Fixed 2026-06-08 in worktree .worktrees/6bm7-prune-fetch on branch fix/6bm7-prune-worktrees-fetch-progress -> PR dhh1128/gitbulk#21 (OPEN). (1) my_open_prs gained on_progress(repos_completed,repos_total) fired per repo-chunk (protocol+Production+Fake); prune_worktrees drives a 'fetching open PRs: N/M' Progress bar off it (TTY-gated; cron stays quiet). (2) On fetch failure: failing gh command recorded in errors.log context (visible via 'gitbulk show prune-worktrees --errors'); summary now carries a transient-5xx hint + run-dir path + the --errors pointer. Abort-is-structural left intact (skipping a 502'd chunk could prune a worktree whose branch has an open PR). 100% cov, full suite green. Leave open until #21 merges; then tick off + drop ~6bm7 marks. Follow-up still noted: _OPEN_PRS_REPO_CHUNK=50 may itself trigger the 502 (heavy per-chunk query).
- 2026-06-08T22:56Z Root-cause mitigations added to PR #21 (commit a3dbf6e), driven by user Q on coping with the 502. DIAGNOSIS (evidence): reproduced gitbulk's exact pattern (heavy query, author=None, back-to-back 50-repo chunks, no sleep) across all 191 fleet repos -> all chunks succeed 2-4s; could NOT repro the 502 (GitHub status all-operational at test time). => 502 is GitHub-side bad-gateway on the expensive search, NOT request frequency; sleeps wouldn't help (throttling = 429+Retry-After, already handled w/ backoff). Aggravator: prune only uses pr.head_ref but the query pulled reviewThreads(100)/commits/checks/timeline/labels for every PR, author=None = everyone's PRs. FIXES (all 3 levers, user chose all): (1) new GHClient.open_pr_heads() lean GraphQL (headRefName only) -> dict[slug,set[head]]; my_open_prs+open_pr_heads share _chunked_pr_search. (2) _OPEN_PRS_REPO_CHUNK 50->20. (3) max_retries 3->5 + clamp exponential backoff term. LIVE dry-run via worktree venv against real 190-repo fleet: exit 0, clean, no errors.log, ~106s (run 20260608T225255Z). 100% cov, 1959 tests. Honest caveat: can't prove 502 gone (not reproducible now); win is 'provoke far less + ride out short blips'; sustained GitHub incident -> the improved error is the answer.
