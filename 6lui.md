# gitbulk recover/undo subcommand: first-class restore of branches deleted by prune-branches --apply
kind: idea
tags: prune, recovery
created: 2026-06-07T04:29Z
closed: 2026-06-08T21:16Z

- 2026-06-07T04:29Z Promote the recover-branch script (see [[7slq]]) into gitbulk itself, e.g. 'gitbulk recover-branch <slug> <branch>' / 'gitbulk undo prune-branches [--run <id>]'. All data needed already exists in run state: state.yaml branch rows carry sha + disposition (deleted/already-gone), and errors.log has the deleted-branch audit events with full SHA + PR. Subcommand would: locate the deletion in the latest (or named) run, re-POST the ref, and report. Consider a dry-run/confirm gate and a batch 'undo whole run' mode. Context: prune-branches gained a sacred-branch backstop in commits 80f04a8 (worktrees) + a9f23ae (branches); an audit of all 13 historical prune-branches runs (511 deletions) found ZERO main/master/default-branch deletions, so this is a safety-net feature, not a cleanup of past damage.
- 2026-06-08T16:51Z Rescoped 2026-06-08 to the single build item (7slq retired as superseded). Build ONE flat subcommand 'gitbulk recover-branch' — matches the existing verb-noun taxonomy (prune-branches, prune-worktrees, close-stale, rebase-pr) rather than introducing an 'undo <command>' namespace. Scope is controlled by args/flags on that one command, NOT two separate commands: 'recover-branch <slug> <branch>' restores one branch; 'recover-branch --run <id>' (no branch) restores every branch deleted in that run; dry-run by default with an --apply gate mirroring prune's UX. Backed by an internal gitbulk/recover.py for the testable core; reuses runstate.py (run-dir discovery + state.yaml 'deleted' rows carrying {slug,branch,sha} + errors.log deleted-branch events) and gh.py (add a create-ref counterpart to delete_branch_ref). Naming 'recover-branch' is the recommendation; 'undo prune-branches' was considered and set aside to avoid committing to an undo namespace — revisit only if a broader undo framework emerges.
- 2026-06-08T17:11Z Implemented 2026-06-08 (TDD-first, NOT yet committed — on working tree). New: gitbulk/recover.py (pure core: collect_deleted + recover_one, idempotent via branch_ref_sha pre-check, GHError→failed outcome) and commands/recover_branch.py (source-run resolution, dry-run default + --apply, own RunState audit trail, exit 0/1/2). Added GHClient.create_branch_ref to Protocol + FakeGHClient (create_branch_calls/create_branch_responses) + ProductionGHClient (POST git/refs, verified non-deprecated 2026-06-08). Wired into subcommands.KNOWN (mutating, needs_clone=False, chain=()) + cli.py (handler, _add_recover_branch_args, build_parser). Tests: test_recover.py (core), test_recover_branch.py (handler, fabricated state.yaml under isolated_xdg + FakeGHClient — no network/no real pruned branch), production create_branch_ref tests; updated test_subcommands.py + test_cli.py registry tables. 100% coverage on all three modules; full suite green. Docs: commands.md table row + recover-branch section. CLI verified: 'gitbulk recover-branch --help' + dry-run.
